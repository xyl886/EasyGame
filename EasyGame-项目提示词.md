# EasyGame 项目开发提示词（Prompt）

> 版本：v1.0 | 更新日期：2026-08-08 | 项目代号：EasyGame-MVP

---

## 一、项目概述

### 1.1 项目定位
**EasyGame** 是一个轻量级的 **益智小游戏集合** Web 应用，主打「即开即玩、无需安装、全家同乐」。首期以经典 **2048** 为核心标杆游戏，后续逐步扩展更多益智小游戏。

### 1.2 核心目标
| 目标 | 说明 |
|------|------|
| 🎯 MVP 目标 | 上线 2048 游戏，支持 PC/手机浏览器、同 WiFi 局域网访问 |
| 📱 中期目标 | 扩展 4~6 款经典益智小游戏，支持 PWA 离线使用 |
| 🚀 长期目标 | 平滑迁移至微信小程序，双端同步运营 |

### 1.3 参考标杆
- 玩法 & UI 风格参考：**https://2048.gg/zh**
- 游戏体验要求：流畅、简洁、无广告、无干扰

---

## 二、技术栈选型（MVP 阶段）

### 2.1 核心技术栈
| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|----------|
| 框架 | **Vue 3** | ≥ 3.4 | Composition API + 响应式性能优异，生态成熟 |
| 语言 | **TypeScript** | ≥ 5.0 | 类型安全，便于后期重构和小程序迁移 |
| 构建 | **Vite** | ≥ 5.0 | 极速启动 / HMR，支持 `host: 0.0.0.0` 局域网访问 |
| 样式 | **Tailwind CSS 3** | ≥ 3.4 | 原子化 CSS，快速构建响应式 UI，主题切换方便 |
| 状态 | **Pinia** | ≥ 2.1 | Vue 3 官方推荐，轻量且 TS 友好 |
| 路由 | **Vue Router** | ≥ 4.3 | 多游戏页面切换 |
| 包管理 | **pnpm** | ≥ 8.0 | 节省磁盘空间，安装速度快 |

### 2.2 可选增强（MVP 后期集成）
| 功能 | 方案 | 说明 |
|------|------|------|
| PWA | `vite-plugin-pwa` | 离线访问 + 添加到桌面 |
| 二维码 | `qrcode` | 生成局域网访问地址扫码 |
| 局域网 IP | Node `os` 模块 | 自动检测本机 IP 并显示 |
| 音效 | Web Audio API | 游戏音效，可开关 |

---

## 三、游戏列表规划

### 3.1 Phase 1（MVP，优先实现）
| 游戏 | 优先级 | 参考玩法 | 预计工作量 |
|------|--------|----------|------------|
| **2048** | P0 必须 | 2048.gg 经典版 | 2~3 天 |

### 3.2 Phase 2（扩展期）
| 游戏 | 优先级 | 说明 |
|------|--------|------|
| 俄罗斯方块 | P1 | 经典消除 |
| 扫雷 | P1 | Windows 经典版 |
| 数独 | P1 | 9x9 标准数独 |
| 贪吃蛇 | P2 | 经典像素风 |
| 华容道 | P2 | 数字滑块版 |

---

## 四、核心功能需求

### 4.1 2048 游戏功能（P0）
- [x] 4x4 棋盘，滑动/键盘方向键合并数字
- [x] 分数统计 + 最高分记录（localStorage 持久化）
- [x] 游戏结束检测（无可移动格子）
- [x] 胜利检测（合成 2048，可继续玩）
- [x] 重新开始按钮
- [x] 撤销一步（可选项，体验加分）
- [x] 流畅的方块合并动画
- [x] 键盘 ↑↓←→ / WASD 支持
- [x] 移动端触摸滑动手势支持

### 4.2 全局功能
- [x] 首页游戏大厅（展示所有游戏入口卡片）
- [x] 深色 / 浅色主题切换，持久化选择
- [x] 全响应式：PC / 平板 / 手机三端自适应
- [x] 局域网访问：显示本机 IP + 端口 + 二维码
- [x] 无后端依赖，纯静态页面即可运行

---

## 五、局域网访问方案（重点）

### 5.1 技术实现
```typescript
// vite.config.ts
export default defineConfig({
  server: {
    host: '0.0.0.0',   // 监听所有网卡，允许局域网访问
    port: 5173,
    strictPort: true,
  },
});
```

### 5.2 UI 展示要求
在首页底部或侧边增加「局域网访问」面板：
1. **自动检测 IP**：通过 Vite 插件或启动时 Node `os.networkInterfaces()` 读取本机局域网 IPv4
2. **显示访问地址**：如 `http://192.168.1.100:5173`
3. **生成二维码**：将地址转为 QR Code，手机浏览器扫码直达
4. **一键复制**：点击复制 IP 地址到剪贴板
5. **防火墙提示**：若无法访问，提示「请检查 Windows 防火墙是否允许 5173 端口」

---

## 六、微信小程序适配策略

### 6.1 架构预留原则
> **关键**：游戏逻辑层与视图层必须严格分离，小程序迁移时只需替换视图层。

```
src/
├── game/                    # 游戏纯逻辑层（纯 TS，零 DOM 依赖）—— 迁移时直接复用
│   ├── 2048/
│   │   ├── GameEngine.ts    # 核心状态机（移动、合并、计分）
│   │   └── types.ts         # 类型定义
│   └── base/
│       └── BaseGame.ts      # 游戏基类接口
├── adapters/                # 平台适配层（封装差异 API）
│   ├── StorageAdapter.ts    # 存储：localStorage ↔ wx.setStorageSync
│   └── PlatformAdapter.ts   # 平台信息：浏览器 ↔ 小程序
├── views/                   # 视图层（迁移时改造成小程序页面）
│   ├── Home.vue             # ↔ pages/home/home.wxml
│   └── games/2048.vue       # ↔ pages/game-2048/index.wxml
└── stores/                  # Pinia 状态（迁移时可替换为小程序 globalData）
```

### 6.2 迁移路线图
1. **H5 阶段**：按上述架构开发，确保 `game/` 和 `adapters/` 完全不依赖浏览器 DOM
2. **迁移阶段**：使用 **Taro 3** 或 **uni-app** 重建 `views/` 层，复用逻辑层 90% 代码
3. **适配阶段**：处理小程序存储、分享、广告组件等平台特有能力

---

## 七、UI/UX 设计规范

### 7.1 配色系统
| 主题 | 背景色 | 卡片色 | 主文字 | 强调色 |
|------|--------|--------|--------|--------|
| **浅色（默认）** | `#faf8ef`（暖米色） | `#ffffff` | `#776e65` | `#edc22e` |
| **深色** | `#1a1a2e` | `#252542` | `#eaeaea` | `#f2b93a` |

### 7.2 2048 方块配色（沿用经典）
| 数字 | 背景色 | 文字色 |
|------|--------|--------|
| 2    | `#eee4da` | `#776e65` |
| 4    | `#ede0c8` | `#776e65` |
| 8    | `#f2b179` | `#f9f6f2` |
| 16   | `#f59563` | `#f9f6f2` |
| 32   | `#f67c5f` | `#f9f6f2` |
| 64   | `#f65e3b` | `#f9f6f2` |
| 128  | `#edcf72` | `#f9f6f2` |
| 256  | `#edcc61` | `#f9f6f2` |
| 512  | `#edc850` | `#f9f6f2` |
| 1024 | `#edc53f` | `#f9f6f2` |
| 2048 | `#edc22e` | `#f9f6f2` |

### 7.3 组件风格
- 圆角：`rounded-2xl`（16px）卡片，`rounded-xl`（12px）按钮
- 阴影：`shadow-lg` 柔和阴影，悬停时 `shadow-xl`
- 动效：`transition-all duration-200 ease-out` 平滑过渡
- 字体：无衬线系统字体栈 `Inter, system-ui, -apple-system, sans-serif`

### 7.4 响应式断点
| 设备 | 宽度 | 布局 |
|------|------|------|
| 手机 | `< 768px` | 单列，棋盘全屏宽度 |
| 平板 | `768px ~ 1024px` | 单列，棋盘最大 480px |
| PC | `≥ 1024px` | 居中卡片，最大宽度 1200px |

---

## 八、项目目录结构

```
EasyGame/
├── public/
│   └── favicon.svg
├── src/
│   ├── assets/                 # 静态资源（图标、音效）
│   │   └── styles/
│   │       └── main.css        # Tailwind 入口 + 全局样式
│   ├── game/                   # 游戏逻辑层（纯 TS，无 DOM 依赖）
│   │   ├── 2048/
│   │   │   ├── GameEngine.ts   # 2048 核心引擎
│   │   │   └── types.ts
│   │   └── base/
│   │       └── BaseGame.ts     # 游戏基类接口
│   ├── adapters/               # 平台适配层
│   │   ├── StorageAdapter.ts
│   │   └── PlatformAdapter.ts
│   ├── stores/                 # Pinia 状态管理
│   │   ├── theme.ts            # 主题切换
│   │   └── game.ts             # 全局游戏状态
│   ├── views/                  # 页面组件
│   │   ├── Home.vue            # 首页大厅
│   │   └── games/
│   │       └── Game2048.vue    # 2048 游戏页面
│   ├── components/             # 通用组件
│   │   ├── GameCard.vue        # 游戏入口卡片
│   │   ├── ThemeToggle.vue     # 主题切换
│   │   └── LanPanel.vue        # 局域网访问面板
│   ├── router/                 # 路由
│   │   └── index.ts
│   ├── utils/                  # 工具函数
│   │   ├── ip.ts               # IP 检测（构建时用）
│   │   └── qrcode.ts           # 二维码生成
│   ├── App.vue
│   └── main.ts
├── index.html
├── vite.config.ts
├── tailwind.config.ts
├── postcss.config.js
├── tsconfig.json
├── package.json
└── pnpm-lock.yaml
```

---

## 九、开发阶段规划

### Phase 1：MVP 基础版（3~5 天）
- [ ] 项目初始化 + 技术栈集成
- [ ] 2048 游戏核心逻辑 + 视图
- [ ] 首页游戏大厅（展示 2048 入口）
- [ ] 深色 / 浅色主题切换
- [ ] 局域网访问面板（IP + 二维码 + 复制）
- [ ] 响应式适配三端

### Phase 2：体验优化（2~3 天）
- [x] 撤销功能
- [x] PWA 配置（离线 + 添加到桌面）
- [ ] 游戏音效 + 音效开关
- [ ] 分享功能（Web Share API）
- [x] 404 页面 + 加载态

### Phase 3：游戏拓展（每款 1~2 天）
- [ ] 俄罗斯方块
- [ ] 扫雷
- [ ] 数独
- [ ] 贪吃蛇
- [ ] 华容道

### Phase 4：小程序迁移（5~7 天）
- [ ] 评估 Taro 3 vs uni-app，选定方案
- [ ] 视图层迁移（Vue → 小程序模板）
- [ ] 适配层完善（Storage / Share / 支付等）
- [ ] 小程序发布流程配置

---

## 十、代码规范

### 10.1 通用原则
- 使用 Composition API + `<script setup lang="ts">`
- 文件名：组件 PascalCase（`GameCard.vue`），工具 kebab-case（`ip-utils.ts`）
- Props / Emits 必须声明类型，禁止 any
- 游戏逻辑层（`game/`）禁止引入 `vue` / `document` / `window`

### 10.2 Git 提交规范
```
feat: 新增 2048 游戏引擎
fix: 修复移动端滑动方向判断错误
style: 调整深色主题方块颜色
refactor: 抽离 StorageAdapter 适配层
docs: 更新项目提示词文档
```

---

## 十一、快速初始化命令（参考）

```bash
# 1. 初始化 Vite + Vue 3 + TS
pnpm create vite@latest . -- --template vue-ts

# 2. 安装依赖
pnpm add vue-router@4 pinia
pnpm add -D tailwindcss@3 postcss autoprefixer
pnpm add qrcode                    # 二维码生成
pnpm add -D @types/qrcode

# 3. 初始化 Tailwind
npx tailwindcss init -p

# 4. 启动开发服务器（局域网可访问）
pnpm dev
```

---

## 十二、验收 Checklist（MVP 交付标准）

### 功能验收
- [ ] 2048 可正常游玩，合并 / 计分 / 游戏结束 / 胜利逻辑正确
- [ ] PC 端键盘方向键、WASD 可操作
- [ ] 手机端可触摸滑动操作
- [ ] 最高分在刷新后仍保留
- [ ] 深色 / 浅色主题切换正常，刷新后保留选择
- [ ] 首页游戏卡片可点击进入 2048
- [ ] 局域网面板显示正确 IP 和端口
- [ ] 手机扫码二维码可正常访问游戏

### 体验验收
- [ ] 方块合并有流畅动画，无明显卡顿
- [ ] 三端（PC/平板/手机）布局无错位、无溢出
- [ ] 按钮有悬停 / 点击反馈
- [ ] 加载 / 切换页面无白屏闪烁

### 技术验收
- [ ] `game/2048/` 目录不依赖 Vue / DOM API
- [ ] 无 TypeScript 报错（`pnpm build` 成功）
- [ ] Tailwind 原子化样式，无冗余自定义 CSS
- [ ] 构建产物为纯静态文件，可直接部署到任意静态托管

---

> 📌 **使用说明**：后续每次开发时，请将此文档作为上下文提供给 AI 助手，并说明当前处于哪个 Phase、正在实现哪个功能，以确保技术选型和架构风格的一致性。
